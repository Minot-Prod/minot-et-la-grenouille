[build]
  command = "npm ci && npm run build"
  publish = "dist"

[context.production]
  command = "npm ci && npm run build"

[context.deploy-preview]
  command = "npm ci && npm run build"
  publish = "dist"

# Plugins optionnels, supprime si ̤a te saoule
# [[plugins]]
#   package = "@netlify/plugin-lighthouse"
#   [plugins.inputs.settings]
#     preset = "desktop"